› What is "adapted-presentation witness (w) for (a)"?


• An adapted-presentation witness (w) is a finite certificate
  expressing the fact that (a\in\delta _5(L)) in a free
  presentation.

  Take the canonical surjection
  [
  F\twoheadrightarrow L,
  ]
  where (F) is the free Lie ring generated by the underlying set
  of (L). The witness contains:

  1. A lift (\widetilde a\in F) such that (\widetilde a\mapsto a),
     with (\widetilde a\in\gamma _3(F)).

  2. An element (h\in U(F)) lying in the fifth power of the
     augmentation ideal.

  3. A finite collection of presentation relations (r\in\ker(F\to
     L)), integer coefficients (n), and words (v\in U(F)),
     satisfying
     [
     \sum n,\iota(r)v=\iota(\widetilde a)-h
     ]
     inside (U(F)).

  The word “adapted” means that this finite relation expression is
  placed into the common Smith-adapted coordinate system used by
  the PBW collector.

  Importantly, (w) is not an additional hypothesis. The theorem
  exists_adaptedPresentationDimensionFiveWitness constructs it
  automatically from the assumption (a\in\delta _5(L)).


› Okay. What does it mean to "prove that it lies in \gamma_3"?
  That's obvious, because it lies in \delta_5 < \delta_4 <
  \delta_3 = \gamma_3 (the last statement has already been proven
  in our library).


• Exactly. There is no remaining mathematical work in proving
  (a\in\gamma_3(L)):

  [
  a\in\delta_5(L)\subseteq\delta_3(L)=\gamma_3(L).
  ]

  This has already been formalized, and the constructed witness
  also records a lift of (a) lying in (\gamma_3(F)).

  So that should not have appeared as a separate substantive step.
  The only missing bridge is:

  [
  \boxed{\text{the formal coefficient terminalZeta equals the
  already-defined }\gamma_3\text{-coordinate of }a.}
  ]

  Everything before that equality is already available.


› Okay. In that case, only point 5 of your last six-point plan is
  meaningful. Do you clearly understand how to prove it? Can you
  describe how you'll prove it more clearly? Keep it short, but
  be as clear as possible. This shouldn't be just words to
  convince me everything will work out, but a clear action plan
  that you'll then implement, and it should quickly lead to
  success. At the same time, try to add a minimum of intermediate
  lemmas, because intermediate lemmas in Lean, as we've already
  discovered, create unnecessary problems that will never lead to
  the final result.


• Yes. I will prove one main theorem:

  # [
  \operatorname{terminalZeta}(w,0)

  \operatorname{gammaThreeEquiv}(a).
  ]

  The implementation will be:

  1. Define one linear projection from (U(F)) to (\mathbb Z/
     q\mathbb Z). Using the existing free PBW basis, it keeps only
     PBW monomials consisting of one weight-three Lie basis
     element, then maps that element through the already-
     constructed identification with (\gamma_3(L)). Every other
     PBW monomial maps to zero.

  2. Apply this projection directly to the existing equality
     w.terminalPackets_value.

  3. Simplify its right-hand side:
      - w.highWord maps to zero because it has augmentation degree
        at least five;

      - ι(w.lieLift) maps to the (\gamma_3)-coordinate of (a),
        using w.evaluates.

  4. Simplify its left-hand side using the existing exhaustive
     terminal-packet classification. In the same PBW basis, each
     permitted packet contributes exactly its term in
     rawPBWProjection .z; all other packets contribute zero.
     Therefore the left side is terminalZeta w 0.

  I will keep the packet calculation inside one principal
  compatibility theorem, using local facts rather than creating
  another large hierarchy of intermediate lemmas. No new
  hypothesis or change to the existing PBW collection is required.


› So, overall, the plan is quite good. It sounds quite feasible.
  I like that, unlike the previous plans left over in previous
  queries, this one focuses on exactly one coordinate of a single
  element, rather than ten coordinates for different monomials.
  However, it's still a meaningful problem. Before implementing
  this plan, I suggest you think carefully about the plan, each
  point, considering the possible difficulties that might arise
  in each case, and immediately figure out how to deal with them.
  Check whether the necessary methods and fields that allow you
  to prove the desired result are actually implemented. And
  finally, write the same plan, but improved and correct. It's
  still very important to avoid creating unnecessary intermediate
  lemmas! Try to keep branching to a minimum. Because when we
  make complex branching or endlessly add intermediate lemmas, we
  spend a lot of time on each of them and ultimately fail to
  complete the desired logical connection. We need to directly
  prove the desired connection, adding the minimum number of
  intermediate necessary statements. Let me reiterate that the
  last plan isn't inherently bad, the main thing is that it's
  actually feasible. I expect a brief analysis from you, with the
  result being a new, improved plan. Again, you shouldn't just
  say something that will convince me, but actually come up with
  a plan that you'll implement, so that you can leave Standing
  Reduction as a proposal ONLY.


• I’ll verify the proposed projection against the actual PBW,
  grading, quotient, and packet APIs. The key question is whether
  the free-PBW map can be defined directly with existing
  machinery, or whether a simpler already-available semantic map
  should replace it.

• Ran rg -n "def adaptedPBWCoefficient|def free.*PBW|PBW.*Equiv|
  │ universalEnvelopingEquivFreeAlgebra|freeLieLengthComponent|
  │ associativeHigh|augmentation.*5|homogeneousThreeQuotient|
  │ … +2 lines
  └ lie/LieRings/PBW/
    TriangularRepresentation.lean:179:noncomputable def
    … +464 lines (ctrl + t to view transcript)
    FiniteRelationRows.lean:235:  · exact
    freeLieLengthComponent_relationSmithRemainder_eq_zero

• Explored
  └ Read WeightedLedgerExtraction.lean,
         AdaptedHomogeneousClassTwoPBW.lean,
         AdaptedWeightedClassTwoPBW.lean, FiniteHomogeneous.lean
    Search freeLieExactBasis|FreeLieExactBasis|exact.*Basis|hall|
    Hall|
           lyndon|Lyndon|Basis.*FreeLie|freeLie.*basis in LieRings
    Search associativeLengthComponent in DegreeFive

• Ran rg -n -i "dynkin|specht|wever|left.*commut|iterated.*commut|
  │ commutator.*word|word.*bracket|bracket.*word" lie/LieRings
  │ Mathlib 2>/dev/null | head -300; rg -n
  │ … +4 lines
  └ lie/LieRings/PBW/HigginsEmbedding.lean:156:    have hmem :=
    (tensorCommutatorIdeal L).mul_mem_right (tensorWord L bs)
    … +108 lines (ctrl + t to view transcript)
    ResolutionCycle.lean:993:    (c : ResolutionCycleCertificate L
    hclass a) : a = 0 :=

• Explored
  └ Read FinitePlacedResolution.lean, ResolutionCycle.lean,
         DegreeFive.lean
    Search BoundaryNormalEquation|LedgerEvaluation|
           value_eq_zero_of_extraction|
           toResolutionCycleCertificate|
           exists.*extraction|hboundary|hledger in DegreeFive
    Read AdaptedPBWBridge.lean
    Read CartanEilenberg.lean, FiniteHomogeneous.lean,
    LowWeight.lean
    Search FreeModulePBW|freeModulePBW|orderedPBWLinearEquiv in
           LieRings
    Search structure LowHomogeneous|def lowHomogeneous|
           Module.Basis.*LowHomogeneous in
           FiniteHomogeneousFactors.lean
    Read FreeClassTwoKernel.lean
    Search ker.*freeClassTwo|freeClassTwo.*ker|exact.*three|
           three.*exact|gammaThree in FreeClassTwoKernel.lean
    Search isUnit.*ZMod|ZMod.*isUnit|Coprime.*isUnit|three.*unit|
           Nat.Coprime.*2 \^|coprime.*2 \^ in lie

• Ran ls -d lie/.lake/packages/mathlib/Mathlib 2>/dev/null; rg -n
  │ "isUnit.*ZMod|ZMod.*isUnit|Coprime.*isUnit|
  │ isUnit_iff_coprime" lie/.lake/packages/mathlib/Mathlib lie/
  │ … +1 lines
  └ lie/.lake/packages/mathlib/Mathlib
    lie/.lake/packages/mathlib/Mathlib/Analysis/Complex/
    … +68 lines (ctrl + t to view transcript)
    Units.lean:151:theorem isUnit_inv {m : ℕ} {n : ℤ} (h : IsUnit
    (n : ZMod m)) :

• Explored
  └ Search inductive.*Terminal|structure.*Terminal|
           terminalPacket.*class|classif|TerminalPacket|
           terminalShape|
           terminal.*cases|exhaust in
           AdaptedTerminalProjection.lean
    Read AdaptedPlacedTerminal.lean

──────────────────────────────────────────────────────────────────

• After checking the code, I would modify the previous plan in one
  important way: a general weight-three PBW projection is not
  currently packaged. Building it would create another large
  branch. We do not need it.

  Improved plan:

  1. Define one linear “Dynkin coordinate” on (U(F)). On an
     associative word (xyz), it returns the
     (\gamma_3(L))-coordinate of
     [
     [x,[y,z]],
     ]
     and it returns zero on words of every other length.

  2. Use the integral degree-three Dynkin identity: on a
     homogeneous degree-three Lie element (f), this map returns
     three times the coordinate of (f). Only one small
     normalization lemma is needed here. We will not divide by
     (3); multiplication by (3) is cancellable in (\mathbb
     Z/2^\kappa\mathbb Z).

  3. Apply this map directly to w.terminalPackets_value.
      - On the right, highWord vanishes because it has degree at
        least five, while lieLift gives three times the actual
        coordinate of (a).

      - On the left, use the existing terminal packet table. Its
        value is
        [
        3,\texttt{terminalZeta}
        ]
        plus contributions from ordered degree-three class-two PBW
        monomials (xxx and xy). The existing general theorem
        terminalPackets_adaptedPBWCoefficient_eq_zero kills all
        those contributions at once. The surviving bracket
        corrections are already identified by coordinateG_cast,
        coordinateC_cast, and coordinateM_cast.

  4. # Cancel (3), obtaining the single desired bridge:
     [
     \texttt{terminalZeta}(w,0)

     \text{the actual }\gamma_3\text{-coordinate of }a.
     ]

  5. Combine it with the existing terminalZeta_eq_zero. The final
     reduced theorem will construct (w) from (a\in\delta_5(L)), so
     its only structural input will be StandingReductionData.

  This uses one new linear map, one short Dynkin normalization
  lemma, and one main bridge theorem. The packet calculation will
  remain inside that bridge theorem rather than spawning separate
  families of lemmas.

─ Worked for 3m 54s ──────────────────────────────────────────────


› What is Dynkin's identity, and can you prove it easily? The
  same question about the normalization lemma and the rest of the
  construction of this linear functional.


• Dynkin’s degree-three identity is elementary.

  For an associative word (xyz), define
  [
  D_3(xyz)=[x,[y,z]],
  ]
  and extend (D_3) linearly, setting it to zero on words whose
  length is not three. Then, for every homogeneous degree-three
  Lie element (f),
  [
  D_3(f)=3f.
  ]

  For example, if (f=[x,[y,z]]), expand (f) inside the enveloping
  algebra:
  [
  f=xyz-xzy-yzx+zyx.
  ]
  Applying (D_3) and using Jacobi gives (3[x,[y,z]]). Every
  degree-three free Lie element is a linear combination of such
  brackets. This proof is straightforward in Lean using the
  existing free-magma homogeneous representation.

  The linear functional will therefore be defined directly on the
  free-word expansion of (U(F)):

  - words of length other than three map to zero;
  - (xyz) maps to the coordinate of ([x,[y,z]]) in
    (\gamma_3(L)\cong\mathbb Z/q\mathbb Z).

  It is only a linear map, not an algebra homomorphism, so its
  construction from the existing free-monoid representation is
  routine.

  # The normalization lemma will state:
  [
  \Phi(\iota(f))

  3,\operatorname{coord}(\operatorname{ev}(f))
  ]
  for (f\in\gamma_3(F)), when the target has class three. The
  existing homogeneous-component lemmas already show that only the
  degree-three component of (f) survives under evaluation; the
  Dynkin identity handles that component. This lemma should be
  reasonably short.

  The genuinely substantial part remains evaluating (\Phi) on the
  terminal ledger. Fortunately, only packets of minimum weight at
  most three can contribute. The existing terminal table reduces
  these to:

  - a weight-one relation with external weights ([]), ([1]),
    ([2]), or ([1,1]);

  - a weight-two relation with external weights ([]) or ([1]);
  - a scalar weight-three relation.

  All higher packets automatically map to zero. The unwanted
  ordered xxx and xy contributions vanish by the already-proved
  general class-two PBW coefficient theorem. The remaining bracket
  terms are exactly three times terminalZeta.

  Thus the Dynkin identity and normalization are not the risky
  part; they are elementary and supported by existing APIs. The
  terminal-ledger calculation is meaningful but confined to one
  short list of packet shapes and one coordinate.


› Okay, answer this question. Suppose you've defined the Dynkin
  map explicitly (as you said) but haven't yet proven any of its
  properties. List the complete list of properties of this map
  that you need to complete the proof. For example, I understand
  that:
  - linearity
  - on a homogeneous element of degree 3, (f) returns (3f).
  - something else.

  Add to this list. The list should NOT be long. It should be the
  most compact, clear list of properties of the Dynkin map that
  (a) are truly easy to prove (but you already said that's not
  the hard part), (b) are sufficient to prove the next (main)
  point (about terminalZeta(w,0)=coord(a)).


• Explored
  └ Read AdaptedCoordinateData.lean

──────────────────────────────────────────────────────────────────

• For the coordinate-valued Dynkin map (\Phi:U(F)\to \mathbb Z/
  q\mathbb Z), only four properties are needed:

  1. Linearity.
     (\Phi) preserves sums and integer multiples. This is
     automatic from its definition as a linear map.

  2. Length support.
     (\Phi) vanishes on associative words of length different from
     three. Consequently, it vanishes on every element of
     filtration degree at least four, including highWord.

  3. Lie normalization.
     For a homogeneous degree-three Lie element (f),
     [
     \Phi(\iota(f))=3,\operatorname{coord}(\operatorname{ev}(f)).
     ]
     More generally, if (f\in\gamma_3(F)), the same formula holds
     in a class-three target because all components of degree at
     least four evaluate to zero. This is the Dynkin identity.

  4. The small product table.
     For adapted elements (x_i) of weight one and (y_k) of weight
     two,
     [
     \Phi(x_i y_k)=2G_{ik},\qquad
     \Phi(y_k x_i)=-G_{ik},
     ]
     and
     [
     \Phi(x_i x_j x_l)
     =\operatorname{coord}\bigl([x_i,[x_j,x_l]]\bigr).
     ]
     Also, for the chosen generator (z) of (\gamma_3),
     [
     \Phi(z)=3.
     ]

  These formulas follow immediately by expanding words and using
  Jacobi. They are sufficient: property 2 removes all high
  packets, property 4 evaluates every remaining terminal packet,
  the existing class-two PBW equations cancel the ordered xxx and
  xy terms, and property 3 identifies the right-hand side with
  three times the coordinate of (a). Finally, (3) is cancellable
  modulo (q=2^\kappa).

──────────────────────────────────────────────────────────────────


› Look closely at these four properties again. You guarantee that
  they are (a) true, (b) feasible to implement?


• Explored
  └ Search coordinateZ|gammaThreeGenerator.*preimage|
  generator.*lift|
           zLift|weightThree.*generator|collected.*3|
           coordinate.*three
           in Adapted*.lean

──────────────────────────────────────────────────────────────────

• Yes, after checking them carefully—but property 4 needs one
  correction.

  1. Linearity: true and routine. The map is defined from the
     free-word Finsupp, so Lean supplies linearity directly.

  2. Length-three support: true. The map explicitly ignores words
     whose length is not three. Existing filtration lemmas then
     show that it kills highWord and every packet of minimum
     weight at least four.

  3. Dynkin normalization: true:
     [
     \Phi(\iota(f))=3,\operatorname{coord}(\operatorname{ev}(f))
     ]
     for homogeneous (f) of degree three. It follows by expanding
     bracketed words and using Jacobi. The existing
     freeLieRepresentative and homogeneous-component machinery are
     sufficient to extend the calculation from bracketed words to
     arbitrary homogeneous elements.

  4. Low-product formulas: true:
     [
     \Phi(x_i y_k)=2G_{ik},\qquad
     \Phi(y_k x_i)=-G_{ik},
     ]
     and
     [
     \Phi(x_i x_j x_l)
     =\operatorname{coord}[x_i,[x_j,x_l]].
     ]
     These require a small homogeneous expansion because the
     chosen bases are abstract, but the necessary representation
     machinery exists.

  The correction is that writing (\Phi(z)=3) is not literally
  well-typed: the formal letter (z) has no chosen lift in (F). We
  do not need such a lift. Whenever an actual degree-three element
  has coordinate (1), property 3 says its image is (3); the
  relation tails already provide actual elements with known
  coordinates.

  So:

  - all required statements are mathematically correct;
  - all necessary APIs are present;
  - their Lean proofs are feasible and localized;
  - no full degree-three PBW construction is required.

  The low-product formulas will take some careful homogeneous
  bookkeeping, but there is no missing mathematical structure or
  unproved theorem blocking them.

─ Worked for 1m 08s ──────────────────────────────────────────────


› Okay, then I propose we proceed as follows. From your answers,
  I understand that the Dynkin map is an excellent tool, and it's
  clear how to define and prove its properties. The substantive
  part is using this tool to prove the key transition: that
  terminalZeta exactly coincides with the coordinates of the
  element a. Therefore, I propose the following proof strategy:
  - define the Dynkin map. If linearity is easy to prove
  automatically, then prove it. If any of the FOUR properties
  follow very simply, then prove them.
  - assume the remaining properties of the Dynkin map are exactly
  from the list you just wrote me. This should be no more than
  exactly those four properties (or fewer, if you proved any of
  them in the previous step of the plan).
  - then, under this assumption, we prove all the remaining key
  steps up to terminalZeta(w,0)=coord(a). At the end of this
  step, we MUST have: [Theorem: Standing Reduction + Dynkin
  Properties => \delta_5 = 0]. There should be no other
  assumptions here. Standing Reduction is the assertion that the
  algebra L is a finite 2-primary algebra of nilpotency class 3
  with cyclic gamma_3 = Z/qZ. DynkinProperties is the assertion
  that a particular defined Dynkin map satisfies at most the four
  properties discussed above. There should be no other
  assumptions. Any further assumptions are a major problem and
  cannot be retained.

  Important 1: When defining assumptions about the properties of
  a Dynkin map, one should not primarily consider the ease of
  proving the remaining part, but rather the ability to
  explicitly and easily prove these properties in the future.

  Important 2: Assumptions about the Dynkin properties should be
  strictly simpler to prove than the current assumption that
  terminalZeta is the coordinate of the element a in \delta_5. If
  you see that these assumptions are more difficult to prove than
  the statement itself, then we won't go down that path, as
  there's no point in complicating the assumptions.

  Now I'm waiting for your assessment of my plan. Do you agree
  that this is a good, promising, and feasible proof strategy?


• Explored
  └ Search inductive AdaptedCollectedRelation|def
           AdaptedCollectedRelation|\.scalar|scalar in
           AdaptedPlacedPackets.lean
    Read AdaptedPlacedPackets.lean
    Search relation = \.remainder|\.remainder|rowRemainder|
           terminal.*remainder in AdaptedPlacedTerminal.lean

──────────────────────────────────────────────────────────────────

• Yes. This is a good and disciplined strategy because it
  separates the elementary Dynkin algebra from the genuinely
  substantive terminal-ledger calculation.

  I recommend the following exact boundary.

  First define the fixed linear map (\Phi). Prove unconditionally:

  - linearity, by construction;
  - vanishing on associative filtration degree at least four.

  Then define DynkinProperties with only three fields:

  1. Degree-three normalization:
     [
     \Phi(\iota(f))=3,\operatorname{coord}(\operatorname{ev}(f))
     ]
     for every homogeneous degree-three (f).

  2. The two mixed-product formulas, combined into one field:
     [
     \Phi(x_i y_k)=2G_{ik},\qquad
     \Phi(y_k x_i)=-G_{ik}.
     ]

  3. The triple-product formula:
     [
     \Phi(x_i x_j x_l)
     =\operatorname{coord}[x_i,[x_j,x_l]].
     ]

  No field should mention terminalZeta, terminal packets, a
  witness (w), an element (a), or (\delta_5). Thus these
  assumptions are strictly local elementary statements about the
  explicitly defined map.

  Under precisely StandingReductionData and DynkinProperties, we
  then prove:

  [
  a\in\delta_5(L)\Longrightarrow a=0.
  ]

  Internally, the proof constructs (w), applies (\Phi) to
  terminalPackets_value, uses the existing terminal table and
  class-two PBW vanishing to obtain
  [
  3,\texttt{terminalZeta}(w,0)=3,\operatorname{coord}(a),
  ]
  cancels (3), and invokes terminalZeta_eq_zero.

  If the terminal calculation appears to require any additional
  property involving packets or terminalZeta, we should not add it
  to DynkinProperties; that would indicate this route has failed
  its intended purpose. With the existing packet table and row
  decompositions, I currently see no need for such an additional
  assumption.

──────────────────────────────────────────────────────────────────


› Okay, then I suggest you take a final look at the three
  properties we add under the Dynkin operator assumption and
  write a proof plan. I'll save this plan so we can prove all
  these properties in the future. The plan should be clear and
  understandable. It should be immediately implementable. I'll
  give it to you directly, but in the future.


• Explored
  └ Search def freeLieExact|abbrev freeLieExact|
           structure.*freeLieExact|def magmaLengthComponent|def
           magmaExact|magma.*induction in FreeLie.lean
    Read FreeLie.lean
    Read FiniteHomogeneous.lean

──────────────────────────────────────────────────────────────────

• Here is the final implementable plan for proving
  DynkinProperties.

  ## 1. Define the map

  For a free associative word (w) in generators from (L), define
  [
  d_3(w)=
  \begin{cases}
  \operatorname{coord}[x,[y,z]],&w=xyz,\
  0,&\text{otherwise}.
  \end{cases}
  ]

  Extend this linearly through
  [
  U(F)\cong \operatorname{FreeAlgebra}_{\mathbb Z}(L)
  \cong \mathbb Z[\operatorname{FreeMonoid}(L)].
  ]

  This gives
  [
  \Phi_R:U(F)\longrightarrow\mathbb Z/q\mathbb Z.
  ]

  Linearity is part of the definition. Directly prove that
  (\Phi_R) vanishes on associative filtration degree at least
  four. Neither fact belongs in DynkinProperties.

  ## 2. Define exactly three properties

  structure DynkinProperties (R : StandingReductionData L) : Prop
  where
    exactThree : ...
    oneTwo : ...
    oneOneOne : ...

  ### Property 1: degree-three normalization

  # For every homogeneous (f\in F_3),
  [
  \Phi_R(\iota(f))

  3,\operatorname{coord}(\operatorname{ev}(f)).
  ]

  Lean formulation should use freeLieExact L 3,
  exactThreeEvaluation, and R.gammaThreeEquiv.

  Proof:

  1. Write (f) as the image of a nonassociative polynomial
     supported on bracketed words of length three.

  2. Use linearity to reduce to one bracketed word.
  3. A bracketed word of length three has one of the two forms
     [
     [x,[y,z]],\qquad [[x,y],z].
     ]

  4. Expand it into four associative words.
  5. Apply the definition of (\Phi_R).
  6. Use antisymmetry and Jacobi to obtain three times the
     original bracket.

  The existing freeLieExact, magmaExact, and freeLieMkLinear
  machinery supplies the required representative.

  ### Property 2: weight-one times weight-two

  For every adapted basis pair (x_i,y_k),
  [
  \Phi_R(\iota(x_i)\iota(y_k))=2G_{ik}.
  ]

  Proof:

  1. Expand (x_i) into degree-one generator terms.
  2. Expand (y_k) into degree-two bracket terms ([u,v]).
  3. For one term,
     [
     x[u,v]=xuv-xvu.
     ]

  4. By the definition of (\Phi_R),
     [
     \Phi_R(xuv-xvu)
     =[x,[u,v]]-[x,[v,u]]
     =2[x,[u,v]].
     ]

  5. Sum linearly and identify the result with R.coordinateGMod i
     k.

  Do not assume the reverse-order formula. Derive it later from
  Property 1 and
  [
  \iota([x_i,y_k])
  =\iota(x_i)\iota(y_k)-\iota(y_k)\iota(x_i).
  ]
  This gives automatically
  [
  \Phi_R(\iota(y_k)\iota(x_i))=-G_{ik}.
  ]

  ### Property 3: three weight-one factors

  # For all adapted degree-one basis elements,
  [
  \Phi_R(\iota(x_i)\iota(x_j)\iota(x_l))

  \operatorname{coord}[x_i,[x_j,x_l]].
  ]

  Proof:

  1. Expand each (x_i,x_j,x_l) into generator terms.
  2. Distribute the product using linearity.
  3. On each generator triple, the equality is exactly the
     definition of (\Phi_R).

  4. Reassemble using trilinearity of the bracket and
     gammaThreeEquiv.

  ## 3. Package the result

  After proving these three theorems, define:

  def dynkinProperties (R : StandingReductionData L) :
      DynkinProperties R where
    exactThree := ...
    oneTwo := ...
    oneOneOne := ...

  Only the three property theorems should be named globally. The
  expansions and case analyses should remain local inside their
  proofs. No theorem in this file should mention terminalZeta,
  terminal packets, a witness, or (\delta_5).

─ Worked for 1m 14s ──────────────────────────────────────────────


› Okay. So, I want to split the reduction of the current
  implication StandingReduction + terminalZeta=coord(a) for a in
  \delta_5 => a = 0 into the implication StandingReduction =>
  \delta_5 = 0, dividing the problem EXACTLY into two parts:
  - reduction to the implication StandingReduction +
  DynkinProperties => \delta_5 = 0
  - proof of the properties and, accordingly, reduction to the
  desired StandingReduction => \delta_5 = 0

  You've already written a plan for how to implement the second
  part. Now write a good, clear plan for implementing the first
  part. The plan, again, should be as correct as possible so that
  it can be implemented directly. Again, you'll be the one
  reading this plan as you go (for example, if the context is
  compactified). It should be a clear, straightforward plan, such
  as, say, DynkinProperties getting \delta_5 = 0. The plan must
  be workable and implementable. As we've already agreed, it
  should create a minimal number of intermediate assertions or
  lemmas. It should prove the desired terminalZeta = coord(a) as
  directly as possible, and then \delta_5 = 0.


• I’ll align the plan with the exact witness, packet-table, row-
  decomposition, and PBW-vanishing APIs already present, so it
  does not conceal another bridge or require an extra hypothesis.

• Explored
  └ Search theorem.*collectedRelationRow|def collectedRelationRow|
           adaptedLowRelationRow|firstRelationTail|
           secondRelationTail|
           terminalPackets_.*Projection|
           terminalPackets_.*Coefficient|
           terminalPreTheta_.*Polynomial|
           terminalPreTheta_.*Equation
           in Adapted*.lean
    Search dimensionSubring.*= ⊥|dimensionSubring.*bot|
           eq_bot.*dimensionSubring|dimensionSubring.*≤ in
           LieRings
    Read Reduction.lean
    Read Certificate.lean, AdaptedRelationTails.lean,
         AdaptedIdentities.lean, AdaptedTerminalCoordinates.lean,
         AdaptedTerminalProjection.lean

──────────────────────────────────────────────────────────────────

• ## Part I plan: StandingReduction + DynkinProperties ⇒ δ₅ = 0

  This part should introduce only two named theorems:

  1. terminalZeta_eq_targetCoordinate
  2. dimensionSubring_five_eq_bot_of_dynkinProperties

  All packet calculations remain local inside the first theorem.

  ### 1. Prove the direct coordinate bridge

  Target statement:

  theorem terminalZeta_eq_targetCoordinate
      (R : StandingReductionData L)
      (hΦ : DynkinProperties R)
      {a : L}
      (w : AdaptedPresentationDimensionFiveWitness L L
        (canonicalFreeLieEvaluation L) a) :
      R.terminalZeta w 0 =
        R.gammaThreeEquiv ⟨a, proof_that_a_mem_gammaThree_from_w⟩

  The proof proceeds as follows.

  #### A. Extend the normalization property locally

  DynkinProperties.exactThree concerns homogeneous degree-three
  elements. Inside the theorem, derive:

  # [
  f\in\gamma_3(F)
  \quad\Longrightarrow\quad
  \Phi(\iota(f))

  3,\operatorname{coord}(\operatorname{ev}(f)).
  ]

  Take the degree-three homogeneous component of (f). The
  remainder has weight at least four, hence:

  - (\Phi) kills it by the already-proved support theorem;
  - its evaluation in (L) vanishes because R.classThree.

  This should be a local have, not a new global lemma.

  #### B. Apply (\Phi) to the terminal equality

  Use the existing theorem

  w.terminalPackets_value

  which says that the terminal ledger evaluates to

  [
  \iota(w.\mathrm{lieLift})-w.\mathrm{highWord}.
  ]

  Apply the linear map (\Phi) to this equality.

  The right-hand side becomes

  [
  3,\operatorname{coord}(a):
  ]

  - highWord is killed because it has associative filtration at
    least five;

  - lieLift lies in (\gamma_3(F));
  - w.evaluates identifies its evaluation with (a).

  #### C. Calculate the left-hand side directly

  Prove, as one local calculation,

  # [
  \Phi!\left(\sum_p n_p,p.\mathrm{value}\right)

  3,\texttt{terminalZeta}(w,0).
  ]

  Use map_finsuppSum and consider one packet in the terminal
  support.

  1. If its total weight is at least four, its value lies in
     associative filtration at least four, so (\Phi) kills it.

  2. A high relation tag has weight five and is therefore also
     killed.

  3. For a row packet of total weight at most three, use the
     existing terminal packet table. The only possible shapes are:
      - weight-one row with external weights
        [
        [],\ [1],\ [2],\ [1,1];
        ]

      - weight-two row with external weights
        [
        [],\ [1];
        ]

      - scalar weight-three row.

  4. Expand weight-one and weight-two rows using the already-
     defined data
     coordinateD, coordinateE, coordinateBValue,
     firstRelationTail, and secondRelationTail.

  5. Apply the three fields of DynkinProperties. After this
     calculation, the ledger value has the form
     [
     3,\texttt{terminalZeta}(w,0)
     +\text{ordered-}xy\text{ error}
     +\text{ordered-}xxx\text{ error}.
     ]

  The (xy)-error is exactly
  [
  2\sum_{i,k}G_{ik},
  \bigl(\text{terminal PBW coefficient of }x_i y_k\bigr).
  ]
  Every coefficient vanishes by the existing theorem

  w.terminalPackets_xyCoefficient_eq_zero

  The (xxx)-error comes only from weight-one rows with two
  external weight-one factors. Terminal ordering gives a sorted
  triple. Group these terms by their class-two PBW exponent and
  use the general theorem

  w.terminalPackets_adaptedPBWCoefficient_eq_zero

  for each exponent of weighted degree three.

  Thus both errors vanish.

  The remaining terms are precisely the formal (z)-coefficient
  obtained by unfolding

  terminalZeta
  terminalLieZ
  terminalPreTheta
  PreTheta.polynomial

  at (t=0). The sign identifications use only the existing
  theorems

  coordinateG_cast
  coordinateC_cast
  coordinateM_cast

  No new packet predicate or coordinate structure should be
  introduced.

  #### D. Cancel the factor (3)

  # Combining B and C gives
  [
  3,\texttt{terminalZeta}(w,0)

  3,\operatorname{coord}(a).
  ]

  Because (q=2^\kappa), (3) is a unit in (\mathbb Z/q\mathbb Z).
  Use ZMod.isUnit_iff_coprime and cancel it. This proves
  terminalZeta_eq_targetCoordinate.

  ### 2. Deduce δ₅ = 0

  Target statement:

  theorem dimensionSubring_five_eq_bot_of_dynkinProperties
      (R : StandingReductionData L)
      (hΦ : DynkinProperties R) :
      dimensionSubring ℤ L 5 = ⊥

  Proof:

  1. Install the finite instance supplied by R.finite_inst.
  2. Rewrite with eq_bot_iff.
  3. Let (a\in\delta_5(L)).
  4. Obtain the witness using

     exists_adaptedPresentationDimensionFiveWitness

  5. Apply terminalZeta_eq_targetCoordinate R hΦ w.
  6. Apply the already-proved

     R.terminalZeta_eq_zero w 0

  7. Conclude that the (\gamma_3)-coordinate of (a) is zero.
  8. Use injectivity of R.gammaThreeEquiv to obtain (a=0).

  The final theorem has exactly two inputs:

  - R : StandingReductionData L;
  - hΦ : DynkinProperties R.

  It must not assume a witness, a collection equality, a
  coordinate equation, or any statement involving terminalZeta.

─ Worked for 2m 50s ──────────────────────────────────────────────
